import { Environment } from '@interfaces/environment';

export const environment: Environment = {
  name: 'local',
  development: true,
  api: 'http://securaz.local:8000/api/',
  base: 'http://securaz.local:8000/',
};
