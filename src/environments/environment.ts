import { Environment } from '@interfaces/environment';

/** This will be replaced on build by Angular configuration. */
export const environment: Environment = {
  name: 'Unset',
  development: true,
  api: '',
  base: '',
};
